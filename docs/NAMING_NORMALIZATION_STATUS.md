# Naming Normalization — Status

Per-concept migration status, derived from `NAMING_NORMALIZATION_CANONICAL_MAP.md`'s `status` column (`proposed` / `approved` / `migrating` / `done`) plus any open `conflict-record`. Updated by the `canonical-naming` and `schema-normalization` skills as each concept progresses — regenerate from the map and the `.claude/ops/records/conflict/` records rather than hand-editing, so it never drifts into a second source of truth.

| Concept | Status | Open conflict? | Notes |
|---|---|---|---|
